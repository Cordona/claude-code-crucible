# Local path example

The config lives at /Users/example/project/config.yaml on a Mac.

A Linux equivalent lives at /home/example/project/config.yaml.

A Windows teammate has it at C:\Users\example\project\config.yaml.

The rest of this document is plain prose.
