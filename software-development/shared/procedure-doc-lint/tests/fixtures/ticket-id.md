# Ticket ID example

This change was requested in PROJ-4821 during planning.

A code block should not be scanned for this pattern:

```text
ABC-123 inside a fence is still flagged today (no fence-exclusion for this check)
```

The rest of this document is plain prose.
