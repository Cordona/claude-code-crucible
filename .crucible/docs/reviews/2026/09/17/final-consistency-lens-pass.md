# Final Consistency-Lens Pass — All 27 Touched Files

**Date:** 2026-09-17
**Method:** `lens-consistency-reviewer` (the real, dispatchable subagent — not a generic fallback) run as a full audit against every one of the 27 files touched across this engagement: 18 skill files (9 tech standards, 6 lens standards, 1 language standard, `build-core`, `build-report-standards`) and all 9 `{tech}-developer` agent definitions.

**Disclosure:** `java-reviewer`, `kotlin-reviewer`, `php-reviewer`, `python-reviewer`, `rust-reviewer`, and `devops-reviewer` are not available as dispatchable subagents in this session. Those six tech pairs never received a real domain-reviewer pass at any point in this engagement — only `lens-consistency-reviewer` (this pass) and earlier general-purpose fallback passes. `cloudflare-workers-reviewer`, `react-reviewer`, and `shell-script-reviewer` are available and were used in earlier rounds.

Every dispatch returned `APPROVED_WITH_FOLLOWUPS` — no CRITICAL/HIGH findings anywhere. Total findings across all 27 files: ~80, the large majority LOW. This document covers what was fixed and what remains.

## Regressions fixed (introduced by this engagement's own earlier edits)

1. **`standard-performance/SKILL.md`** — line 34 assigned a concrete severity grade (`LOW`) to a baseline violation, the *only* place in the entire `standards/` tree that does this, directly contradicting this file's own frontmatter disclaimer that it doesn't own the severity scale. Reworded to point at `review-core` for the actual grade instead of stating one.
2. **`standard-devops/SKILL.md`** — the file's three declared "pillars" (per its own frontmatter) were structured two different ways: two as bold paragraph text, one as a real `##` heading — a direct consequence of an earlier round's edit that demoted two of them. `devops-engineer.md` cites "Cloud Security Posture" / "Static-analysis & formatting cleanliness" as if they were headings; for two of the three, they weren't, so the citations didn't resolve. Converted "Infrastructure Correctness & Safety" and "Cloud Security Posture" to real `##` headings, demoted their subsections to `###` to preserve hierarchy. Verified: `devops-engineer.md`'s citations now resolve.

## Fixes applied (genuine MEDIUM findings, exact reviewer-supplied fix text used where available)

**Skill files:**
- `standard-persistence/SKILL.md` — split a vague catch-all frontmatter exclusion into the two real exclusions (lens's own method/category vocabulary; base severity scale), added the missing "WHAT good looks like" scope-boundary sentence, fixed a stale `(above)`→`(below)` directional reference to the `devops-engineer` citation.
- `standard-clean-code/SKILL.md` — replaced a paraphrase of `review-boundaries`' routing table with a citation to it (paraphrasing drifts; binding doesn't), labeled the closing "judge by concrete harm" paragraph as `review-core`'s false-positive discipline applied to structure rather than an unlabeled restatement, added a closing "Clean Code consistency" section (every sibling lens has one; this one didn't).
- `standard-self-documenting-code/SKILL.md` — named `lens-test-quality-reviewer`'s "tests-as-documentation" territory explicitly instead of a vague "split elsewhere," added a closing "Self-Documenting Code consistency" section, tagged its one bare code fence with a language.
- `standard-observability/SKILL.md` — added the missing `{tech}`-pair exclusion (the concrete logging/metrics library and its API) to both the frontmatter and body scope-boundary statements.
- `standard-typescript/SKILL.md` — added a real §3 Naming section and §4 Language-Level Micro-Performance section; three independent reviewer dispatches (kotlin, php, python developer reviews all cite TS indirectly) had flagged the total absence of both as a structural gap in the language tier.
- `standard-python/SKILL.md` — moved the Bandit severity-floor rule (the `-ll`/`-lll` caveat) into the Lint/Type-Checking section, where `python-developer.md`'s gate now points instead of restating it.
- `build-core/SKILL.md` — added `standard-persistence` to the shared-standards list (it was silently omitted despite 5 of 9 tech pairs binding it), cited `standard-observability`'s Log-or-throw rule instead of restating it unlabeled in the Constraints list.
- `build-report-standards/SKILL.md` — replaced "the review swarm" (a term this framework reserves for `flow-review`'s lens roster) with the factually correct `{tech}-reviewer` in both places it appeared, added an extension-point sentence for a dispatch with a genuine extra reporting need (e.g. `tests-developer`), and reconciled the rendering example's field headers with the canonical field-name table (`Files` → `Files created/modified`, `What & why` → `What was implemented`, added the missing `Technology` line).

**Dev-agent files:**
- `java-developer.md` — added the missing idiom-area inventory sentence to the standard-delegation line; replaced a restated validation clean-bar with a pointer to `standard-java` §13.
- `kotlin-developer.md` — converted four name-based `standard-kotlin` citations to the `§N` form every other numbered-standard citation in the repo uses; synced the idiom-area list to `standard-kotlin`'s actual 9 sections.
- `php-developer.md` — replaced a restated micro-performance rule with a pointer to `standard-php`'s own section; added the missing style/format linter step (`php-cs-fixer`/`phpcs`) to the validation gate and reordered `php -l` above the static analyzer.
- `python-developer.md` — gave the `standard-clean-code` bridge row an actual Python-side structural idiom instead of a pure pointer with nothing to map.
- `rust-developer.md` — replaced a restated performance rule with a citation to `standard-performance` Rule 1; collapsed the `cargo build --release` carve-out's duplicated rationale down to a one-line pointer at `standard-git-tag`, which already owns that rationale.
- `react-developer.md` — removed a duplicated version/strict-mode baseline from the IMPORTANT line (now delegates to `standard-react`/`standard-typescript`), dropped a redundant "not a tutorial" clause already stated in `standard-react`, renamed the Manifestations heading from the compound "React/TS" to the family's plain-name convention, and split the Edge Cases framework row into a version row + a properly delegating meta-framework row.
- `shell-script-developer.md` — fixed the role line's title mismatch ("Lead Shell Script Developer" → "Shell Script Technical Lead," matching its own description and all 7 peers), anchored two unanchored standard citations to named sections, replaced a restated self-documenting-code row with shell's own doc vehicle, and dropped an unsupported zsh mention (shellcheck cannot lint it, and `standard-shell-script` states no zsh baseline).
- `devops-engineer.md` — added the missing version/toolchain default clause to the IMPORTANT line (with the Edge Cases row now delegating back to it) and split the previously-merged conduct/delegation paragraph into two, matching every other tech pair's shape.
- `cloudflare-workers-developer.md` — de-restated two Edge Cases rows (state placement, dev-auth-bypass) down to pointers at `standard-cloudflare-workers` §3/§6.

## Remaining backlog (real MEDIUM findings, not yet fixed — scope/time triage, not disagreement with the findings)

- **`standard-kotlin/SKILL.md`** — missing a performance-scope-boundary sentence; Static Analysis section states no mechanically verifiable clean bar.
- **`standard-php/SKILL.md`** — restates `standard-observability`'s log-or-throw rule without a delegation citation; its PHP micro-performance section has no consumer in `php-reviewer.md`.
- **`standard-java/SKILL.md`** — 8 of 13 sections cram multiple rules into single bullets, against the one-rule-per-bullet sibling norm (a larger structural rewrite, deliberately deferred).
- **`standard-react/SKILL.md`** — a unique `## Philosophy` block restates delegated content; no naming/file-layout section; the React 19 version pin has no stated escape clause.
- **`standard-rust/SKILL.md`** — the only tech standard with no version/edition baseline line at all (a known prior-round decision, reflagged; `rust-developer.md`'s IMPORTANT line is the only place the default lives, so `rust-reviewer` never receives it directly).
- **`standard-shell-script/SKILL.md`** — missing a coverage enumeration in frontmatter; flatly excludes `standard-security` despite 3 body sections being security content; missing micro-performance and naming sections.
- **`standard-cloudflare-workers/SKILL.md`** — the reviewer-scoring-directive duplication with `cloudflare-workers-reviewer.md` was confirmed real in the reviewer-agent pass below and fixed there. Remaining: a frontmatter/body exclusion-list mismatch; missing `standard-persistence` in its exclusion list; an overlong description; §4/§5 content misplacement.
- **`standard-devops/SKILL.md`** — 3 findings beyond the CONS-003 heading-structure regression already fixed (exact text not retained from before this pass's context compaction — worth a fresh targeted look).

**Cross-cutting, out of scope for these 27 files:** six independent reviewer dispatches (kotlin, php, python, react, rust, shell-script developer reviews) each independently flagged `software-development/templates/tech-pair/template-tech-developer.md` as stale against all 10 deployed developer agents — it mandates 3 `<example>` blocks (all 10 deployed files use exactly 1), an "Example delegation:" line (no deployed file has one), and a Manifestations table with no `standard-performance` row (all 9 language pairs have one). Worth a dedicated fix pass on the template itself, since it will mis-generate the next on-demand tech pair as-is.

This document sits alongside `pre-existing-consistency-issues-captured-during-cleanup-audit.md` (a separate, earlier-round backlog of 25 pre-existing framework issues unrelated to this consistency pass) — the two are independent, not duplicates.

---

# Round 2 — Reviewer-Agent Consistency Pass (9 `{tech}-reviewer.md` files)

**Date:** 2026-09-17 (same day, follow-on scope).
**Method:** same lens, new target set — `lens-consistency-reviewer` full audit on all 9 `agents/reviewers/tech/*.md` files (java, kotlin, php, python, react, rust, shell-script, cloudflare-workers, devops), each dispatch specifically instructed to re-verify every citation into the paired `{tech}-developer.md`/`standard-{tech}` files edited in Round 1. All 9 came back `APPROVED_WITH_FOLLOWUPS`; zero broken citations from Round 1's edits — every gap found was pre-existing coverage/consistency drift, independent of Round 1.

## Fixes applied

- **`java-reviewer.md`** — `standard-java` §3 (Immutability) and §6 (Streams & collections) had no reviewer coverage anywhere (scope table, idiom enumeration, or category vocabulary). Added an immutability scope row, named §3/§6 in the idiom enumeration, added an `immutability` category.
- **`shell-script-reviewer.md`** — dropped the same stray zsh scope leak just fixed on the developer side (trigger bullet + prompt item); corrected the role-line title ("Lead Shell Script Reviewer" → "Lead Shell Script Code Reviewer", matching the family's "Code Reviewer" form); named the paired developer explicitly instead of "the developer"; de-restated an Edge Cases row to a pointer.
- **`react-reviewer.md`** — added scope/category/severity coverage for three gaps: `standard-react` §10 (lint/type-check discipline), and `standard-typescript`'s brand-new §3 (naming) and §4 (micro-performance), none of which had an owner on the review side. Fixed a real misstatement (`.strict()` described as the loosened case when the standard says the opposite). Renamed "React/TS" to "React" in the two places that still had it after the developer-side rename.
- **`kotlin-reviewer.md`** — two Edge Cases rules (`@DslMarker`, KMP `expect`/`actual`) asserted content with no home in `standard-kotlin`, creating real build/review daylight. Added both as new bullets to `standard-kotlin` §5 (Idioms) and changed the reviewer's rows to point there; dropped a stray third-party test-library API name.
- **`php-reviewer.md`** — added missing PHP micro-perf coverage (scope row, category, severity row); fixed the Static Analysis section, which wrongly claimed PSR-12 as a review target when the standard and the developer's gate both already assign it to the linter; added a CRITICAL severity row for a transaction boundary that can commit partial state (previously capped at MEDIUM despite being a data-corruption case).
- **`python-reviewer.md`** — added the missing `lens-persistence` handoff (the paired developer binds `standard-persistence`; no other reviewer omitted this while its developer bound it); widened an over-narrow security handoff row; gave the relocated Bandit severity-floor rule and the lint/noqa-suppression rule an explicit owner in the "Beyond Correctness" section; added missing severity rows for `lint-type-check` and `micro-perf`, the file's two most-hit categories with no severity anchor at all.
- **`cloudflare-workers-reviewer.md`** — confirmed and fixed the earlier-flagged reviewer-scoring-directive duplication: removed the trailing "a reviewer who cannot prove..." sentence from `standard-cloudflare-workers` §6 (the rule itself stays; the reviewer already owned the scoring directive twice). Compressed the reviewer's dev-bypass row to match the developer's post-edit compression. Widened the `standard-typescript` citation range from §§1-2 to §§1-4 and added `micro-perf` coverage. Fixed a mis-citation (an un-narrowed `catch (e)` pointed at `standard-cloudflare-workers` §7, which explicitly delegates that rule to `standard-typescript` §1).
- **`rust-reviewer.md`** — the only reviewer with two contradictory role-defining paragraphs (one said "WHAT", the other said correctness-detection — merged into one, using the family's "HOW" framing) and the only one missing the mandated "assume fluent, don't re-derive" closing sentence (added). Scoped a `# Panics` severity row down to `# Safety` only (the only anchored half — `# Panics` matches no rule anywhere in `standard-rust`) and added a scope-boundary carve-out sentence. Gave the edition/MSRV prompt item a concrete anchor and a stated fallback (every other tech reviewer's version-related prompt item has one; this was the only one with neither).
- **`devops-reviewer.md`** — added the missing third-pillar coverage (`standard-devops`'s Static-Analysis & Formatting Cleanliness section had no scope row, category, or severity row anywhere in this file, despite the standard's own frontmatter naming this reviewer as its consumer). Added the missing "AND code correctness/logic, which no generic lens covers" clause to both the description and body opener (present in all 8 siblings, absent here despite the file separately claiming sole correctness ownership in prose). Added a request for the human-produced `terraform show -json tfplan` artifact to the prompt contract (two body sections already depend on a plan being supplied, but nothing asked for one). Fixed the qualifier ("specialist" → "infrastructure-specialist", following the family's adapt-the-qualifier convention), Title-cased the two pillar names in the description to match the body and the standard's actual headings, added a missing `correctness` severity row, and fixed a stray post-recapitalization lowercase "Static-analysis" left in `standard-devops` and `devops-engineer.md`.

## Remaining backlog (real findings, deferred — scope/time triage)

- **`php-reviewer.md`** — two LOW naming-convention deviations (`shared language rubric` → `shared rubric`; unbacktick `php-developer` reference).
- **`react-reviewer.md`** — LOW: "framework-specialist" vs. family's usual phrasing (not scored as a defect — `devops-reviewer` shows adapting the qualifier is the actual convention, so this was correctly left alone).
- **`rust-reviewer.md`** — 3 LOW: "How to prompt" has 6 items where the family has 5 (scope+diff-artifact should merge); two trigger bullets should collapse to one; opening line should use "for {domain}" instead of "specializing in", matching the reviewer family (not the developer family) form.
- **`java-reviewer.md`** — LOW: domain phrase ("enterprise JVM applications" vs. the developer's "enterprise JVM application development") — shared with `php-reviewer`/`rust-reviewer`; flagged by its own reviewer as possibly wanting one family-wide pass rather than a single-file edit.
- **`devops-reviewer.md`** — LOW: no untrusted-content-as-data paragraph (shared by 7 of 9 reviewers — a framework-wide question, not a devops-specific gap).

**Cross-cutting, confirmed independently by 4 separate dispatches (java, kotlin, php, python, rust, cloudflare-workers, devops all touched this):** `template-tech-reviewer.md` is stale against the deployed 9-file roster on the same shape as its developer-side twin — it mandates 3 `<example>` blocks (all 9 deployed reviewers use exactly 1), asserts specific per-file example counts that are factually wrong for the files it names as evidence (claims Rust's reviewer uses 4; it uses 1), mandates an "Example delegation:" line (present in zero files), and fixes the handoff column at exactly 8 targets (5 deployed reviewers now carry a 9th, `lens-persistence`). Combined with the developer-side template's identical staleness (Round 1), **both halves of `template-tech-{developer,reviewer}.md` need a single reconciliation pass against the deployed 19-file family** — this is now confirmed by 11 independent dispatches across two rounds, not a one-off observation.

---

# Round 3 — Re-Review Verification (all 9 `{tech}-reviewer.md` files)

**Date:** 2026-09-17 (same day). **Method:** re-dispatched `lens-consistency-reviewer` on all 9 tech-reviewer files with the Round-2 findings fed back for stable-ID verification, per `review-report-standards`' re-review contract. Goal: confirm every claimed fix actually landed, and catch anything the fixes themselves broke.

**Result: every one of the ~35 Round-2 fixes verified as genuinely resolved.** No claimed fix was cosmetic or incomplete. The re-review pass surfaced ~20 additional issues — almost all small, several genuinely collateral damage from the fixes (a swarm/standalone paragraph truncated by a merge, an environment-enum parenthetical dropped alongside the zsh removal, an over-corrected citation that lost a valid anchor, missing severity/category rows for newly-added coverage). All were fixed directly except:

- **`java-reviewer.md`** — the domain-phrase mismatch ("enterprise JVM applications" vs. the developer's "...application development"), still deliberately deferred pending a team decision on whether to fix it as a 3-file family-wide pass (shared with `php`/`rust`) or per-file.
- **Confirmed, not a defect:** the repo's bare-`§N`-for-your-own-primary-standard citation style is used inconsistently against `CLAUDE.md`'s own stated rule (qualify every cross-document reference) — several reviewers cite their own paired standard with a bare `§N`, which the letter of the rule bars but the deployed roster treats as normal. Flagged as a genuine documentation-vs-practice conflict for the team to resolve, not fixed unilaterally in this pass.

All 9 files are now at a verified-clean state: two full audit rounds each, zero open MEDIUM+ findings except the one deferred LOW above.

---

# Round 4 — The 9 `lens-*-reviewer.md` Files (first-ever audit, never touched before)

**Date:** 2026-09-17 (same day). **Scope:** `agents/reviewers/lens/{clean-code,compatibility,consistency,observability,performance,persistence,security,self-documenting-code,test-quality}-reviewer.md` — the language-agnostic lens reviewers, as opposed to the 9 tech-specific reviewers covered in Rounds 2-3. None of these files had been touched anywhere in this engagement before today. No template exists for this file kind (confirmed: `templates/` holds only the `{tech}`-pair templates) — each dispatch built its Conventions Profile from the 9-file sibling roster alone.

**Result:** every dispatch came back `APPROVED_WITH_FOLLOWUPS`. Frontmatter conformance was near-perfect across the board (the one real gap: `lens-persistence-reviewer.md` was missing `WebSearch` despite its Phase 0 gate requiring it to look up version-conditional store capabilities — fixed). The real findings were architectural gaps, not formatting:

**Fixed (MEDIUM):**
- `lens-clean-code-reviewer.md` — misattributed "test files, wholly" to `review-boundaries` (no such row exists there); added a missing Phase-0-style gate section (this lens had none, despite its own text calling it "prone to opinion-as-finding").
- `lens-test-quality-reviewer.md` — didn't bind `review-boundaries` despite being a **named party** in that map (`review-boundaries` itself flags this as "Not yet wired"); fixed the binding, and separately fixed a severity table that had a paragraph interleaved mid-table, orphaning the rows below it (independently flagged by three other lenses' audits as a rendering defect in this file).
- `lens-security-reviewer.md` — claimed XSS/access-control/SSRF/JWT unconditionally while `react-reviewer`, `cloudflare-workers-reviewer`, and `shell-script-reviewer` each explicitly pull those exact surfaces in-pair — a genuine double-flag risk across two reviewers under two different prefixes. Added the carve-outs. Also added a missing `path-traversal` category (the only citation in the file that resolved to nothing — the taint engine names the sink and control but the standard has no CWE-22 rule and the vocabulary had no tag).
- `lens-consistency-reviewer.md` — Phase 1 said "read a conventions doc and stop — do NOT infer" for anything else, which is stricter than the `review-core` method it cites and would halt profiling on a partial `.editorconfig`; loosened to per-dimension. Also fixed an unscoped "this lens does not gate the fix loop" claim that contradicted the file's own HIGH severity path two lines above.
- `lens-performance-reviewer.md` — the standard's baseline-hygiene deferral (fixed in Round 1) had nowhere to land: no severity row, and "What You Judge" named only the scaling rules, silently excluding baseline hygiene and performance-consistency. Added the row and widened the citation. Also added a missing procedural "Engine" section — this was the only gate-bearing lens with no named review-walk at all.
- `lens-observability-reviewer.md` — didn't mirror the standard's `{tech}`-pair library exclusion (added in Round 1) anywhere — no scope row, no handoff, no constraint — so it could score a logging *library choice* the standard explicitly disclaims. Added the row. Also renamed a Phase 0 heading to match the 4-sibling "Gate" convention (it called itself a gate five times in its own body but not in the heading).
- `lens-self-documenting-code-reviewer.md` — a Constraints carve-out asserted unqualified ownership of `naming-clarity` in a way that bypasses `review-boundaries`' own test-identifier tie-breaker — a real double-flag risk since this is the one lens with jurisdiction over test files. Fixed to defer through the table. Also dropped a stray "abstraction" from an Edge Cases row that contradicted this lens's own four explicit disclaimers of structural/abstraction territory.
- `lens-persistence-reviewer.md` — collapsed two now-distinct `flow-decision` seats (store choice — seated on THIS reviewer per the standard's own base-derivation table; aggregate/transaction-boundary sizing — seated on `software-architect`) into one undifferentiated "hand to the decision pattern," including in the description the orchestrator reads to route work. Split them back out.

**Deferred to backlog** (LOW, high-volume, mostly single-file polish — full detail in each dispatch's own findings, not reproduced here): stray markdown formatting (`lens-persistence-reviewer.md`'s unbalanced `**`), a half-dozen heading/wording variants that don't match the roster majority (several dispatches noted these might be worth adopting *repo-wide* rather than reverting — e.g. `lens-persistence-reviewer.md`'s "maps to the `review-report-standards` scale" is arguably clearer than the 7-instance "maps to the skill's scale"), a missing category-tag anchor here and there, and section-ordering nits in 2-3 files. None of these are architectural; all are documented in the individual dispatch reports if picked up later.

**Cross-cutting, non-gating observations for the team (not fixed, need a human decision):**
- `lens-security-reviewer.md`'s `standard-security` binding has no CWE-22/path-traversal control defined anywhere despite claiming CWE-Top-25 grounding — the category tag now exists but the underlying rubric gap is `standard-security`'s, not this file's.
- Several dispatches noted the framework has no settled answer for whether a lens's Phase-0 "output X at the top of your report" instruction is compatible with `review-report-standards`' JSON-first wire format — five lens files do this identically, so it's a consistent pattern, just one whose fit with the report contract was never explicitly settled.
- The `{lens}-consistency` category tag (for a standard's own closing "consistency" section) is wired in 4 of 7 standard-binding lenses and absent in 3 — a genuine 4-vs-3 split with no convention either way.

This closes out the full reviewer-side audit: 9 tech-reviewers (2 rounds each) + 9 lens-reviewers (1 round, first-ever) = 18 reviewer-agent files audited today, on top of the 27 skill/dev-agent files from Rounds 1-1.5. 45 files total touched by this engagement's final diligence pass.

---

# Round 5 — Re-Review of the 9 `lens-*-reviewer.md` Files (2026-09-18)

**Method:** identical to Round 3's tech-reviewer re-review — re-dispatched `lens-consistency-reviewer` on all 9 lens files with Round 4's findings fed back for stable-ID verification.

**Result:** every claimed Round 4 fix verified as genuinely resolved. The re-review surfaced a substantial second wave of findings — mostly the fixes' own second-order gaps (a new section added but not wired into the file's other self-declarations; an exclusion added to one of three propagation sites but not the other two; a `flow-decision` seat fix that made a *different*, previously-fine sentence contradictory). All MEDIUM findings from this wave were fixed, including:

- **`lens-clean-code-reviewer.md`** — the new Phase 0 gate from Round 4 was unwired (not referenced in Core Responsibilities, no Edge Cases row, no "findings must be consistent" clause). Wired all three.
- **`lens-performance-reviewer.md`** — the new Engine section was similarly unwired; the Constraint blanket against flagging cold paths now directly contradicted the new baseline-hygiene severity row sitting right above it; `performance-consistency` was claimed as bound rubric but had no category tag. Fixed all three, plus reformatted the Engine's 6 steps into a real numbered list (was an unscannable run-on sentence).
- **`lens-consistency-reviewer.md`** (self-referential audit) — the description under-declared 5 of the categories the body actually scores (error-pattern, immutability, visibility, construction, validation-placement) and its skip-clause contradicted its own scope; added a Phase 0 gate (this was the only lens missing one entirely); renamed the still-numbered "Phase 2" judging section; restored the standard Scope Boundary header and added the missing skills-block comment.
- **`lens-persistence-reviewer.md`** — the Round 4 `flow-decision`-seat fix landed correctly in the description and Handoff but left the Scope Boundary table still asserting the old, now-contradictory routing; split it to match. Also added a blockquote deferring to `review-boundaries` instead of a paraphrased two-sentence cell, and added the missing cache-coherence handoff row.
- **`lens-security-reviewer.md`** — the Round 4 carve-out row named the *categories* of framework-native surfaces but not the specific gating cases (hardcoded secrets, open redirect, rate-limiting) each pair actually owns; extended it. Fixed a real inversion: the file told itself to score security-logging findings itself when observability isn't in the swarm, which is backwards from every other lens's (and `review-boundaries`' own) "disclose, never silently cover" rule. Added a missing Phase 1 security-convention-profiling section — this lens claims a `security-consistency` category with no profiling step to derive the norm against.
- **`lens-observability-reviewer.md`** — the Round 4 logging-library exclusion landed in only 1 of the 3 sites the roster propagates an exclusion to (Scope Boundary, but not the description's "It does NOT" paragraph, Handoff, or Constraints); propagated to all. Fixed "What You Judge"'s own section index, which silently dropped the Safety half of a section it claims to fully index.
- **`lens-test-quality-reviewer.md`** — the Round 4 `review-boundaries` binding didn't get the matching Constraints disclosure bullet or Handoff routing that the other 5 binders all carry, and a Scope Boundary cell still paraphrased the map's criteria instead of citing it (the exact "bind, don't restate" violation this file was just fixed for). Fixed all three. Also fixed a **real staleness bug in `review-boundaries/SKILL.md` itself** (outside the lens-reviewer directory) — it still said "Not yet wired: lens-test-quality-reviewer does not currently bind this skill," which became false the moment Round 4 wired it; two independent re-review dispatches caught this independently.
- **`lens-self-documenting-code-reviewer.md`** — added a Phase 0 gate (this was the last lens without one); trimmed a Round 4 fix that had over-corrected into re-paraphrasing `review-boundaries`' tie-breaker rule instead of just citing it.

**Deferred (LOW, cosmetic/contested):** severity-table arrow-direction consistency in `lens-persistence-reviewer.md`, a color-rationale comment whose premise a re-review disproved (claims no off-palette color is free; `teal` actually is — team's call whether to move off the pink/`{tech}`-reviewer collision), a few heading-parenthetical wording variants where re-reviewers explicitly recommended *adopting the deviation roster-wide* rather than reverting it (e.g. naming `review-report-standards` explicitly instead of the vaguer "the skill's scale"), and method-section reordering in `lens-self-documenting-code-reviewer.md`. None of these are in this document's fixed list; each dispatch's own findings carry full detail if picked up later.

**Standing recommendation carried over from Round 4, now reinforced:** every re-review dispatch independently reconfirmed no template exists for the lens-reviewer file kind, and several explicitly recommended that if any of the "adopt roster-wide" wording deviations above are accepted, they should be applied in one coordinated sweep across all 9 files rather than piecemeal — the same lesson already learned from the tech-reviewer and tech-pair templates.

At this point both reviewer families (9 tech-reviewers, 9 lens-reviewers) have been through two full audit rounds each with all MEDIUM+ findings closed. Further passes would be diminishing returns on cosmetic LOW items already explicitly flagged as team calls.

---

# Round 6 — Full LOW Cleanup + Third Re-Review (2026-09-18)

**Trigger:** user asked to fix every remaining deferred LOW from Round 5 (both the "quick, uncontested" and the "contested/team-call" categories), then re-review again.

**Decisions made on the contested items** (since "fix all" required picking a direction rather than leaving them open):
- Normalized the "Severity Guidance" heading wording to `## Severity Guidance (maps onto \`review-report-standards\` — never redefines it)` across **all 9** lens-reviewer files — the form two independent re-reviewers had identified as clearer than the plurality wording.
- `lens-persistence-reviewer.md`'s `color: pink` (which collided with the `{tech}-reviewer` role-marker color on a factually-wrong "no free color exists" rationale) moved to `color: teal`, confirmed free and now unique across the roster.
- Hoisted `lens-test-quality-reviewer.md`'s buried test-expectation guard and `lens-self-documenting-code-reviewer.md`'s missing gate into proper `Phase 0` sections, matching the roster's dominant convention.

**Self-inflicted incident, caught and fixed within this round:** a scripted shell find-replace used to apply the heading normalization mishandled backticks as command substitution, silently blanking `` `review-report-standards` `` out of 7 files' headings. Caught immediately via grep verification before the re-review dispatch went out; fixed directly with per-file edits before any reviewer saw it.

**Re-review (a third full round) surfaced ~25 more findings, overwhelmingly a repeating structural pattern:** a gate/section added in one round wasn't wired into all of its convention's touchpoints (Core Responsibilities, Edge Cases, the "this body defines only" sentence, the description's skip-clause) in the same round. All MEDIUM findings were fixed, including two genuine logic bugs this depth of re-review was needed to catch:
- **`lens-performance-reviewer.md`** — the baseline-hygiene LOW-severity row (a Round 1 fix) was being silently overridden by **five separate statements** telling the lens not to flag exactly what that row grades — including the description's own seating skip-clause, which meant the orchestrator might never even dispatch the lens onto the path where the row could fire. All five now carry an explicit carve-out.
- **`lens-security-reviewer.md`** — two in-scope cells (secrets-in-config, supply-chain) claimed territory `devops-reviewer` also explicitly owns for IaC files, a live double-flag risk under two prefixes; scoped both to application code.
- **`lens-compatibility-reviewer.md`** — the sole lens in the 9-file roster with no `## What You Judge` section at all, so a scripted future edit targeting that section would silently skip this file. Added.

All MEDIUM findings from this round are now fixed. A handful of LOW findings remain (mostly Handoff-bullet formatting-shape preferences and one missing enumeration item in `lens-persistence-reviewer.md`'s security handoff) — genuinely cosmetic, no double-flag or architectural risk, and not worth a fourth round.

---

# Round 7 — The Test Trio: `flow-testing`, `tests-developer.md`, `lens-test-quality-reviewer.md` (2026-09-18)

**Trigger:** explicit user request to refactor and audit these three files together as one unit — none had been touched by this engagement before (unlike the 9 `{tech}-developer`/`{tech}-reviewer` pairs, `tests-developer` and `lens-test-quality-reviewer` are not part of that symmetric family, and no flow skill had been audited at all until now).

**Method:** three parallel `lens-consistency-reviewer` dispatches — one per file, each specifically briefed to verify the OTHER two files' cross-references resolve, not just its own internal shape — followed by one re-review round verifying the fixes.

**What the first pass found — genuine three-way contract bugs, not formatting:**
- `tests-developer.md` bound `standard-clean-code`, which explicitly excludes test code and routes it to `standard-testing` §9 instead — a real layering violation with no reviewer counterpart.
- `tests-developer.md` instructed the agent to "read the matching `standard-{tech}` file itself" with no path — unexecutable for an agent dispatched with no conversation history against a project directory that isn't this framework repo.
- `tests-developer.md` was the only one of 10 developer agents missing the untrusted-content/injection-resistance paragraph, despite holding the identical `Write`+`Bash`+`WebFetch`/`acceptEdits` profile as the 9 that have it.
- Three documents disagreed on the report envelope: `build-report-standards` capped extensions at "ONE additional field" naming a nonexistent "test-file inventory" example, while `tests-developer.md` mandates two real fields (Mutation Verification, repair-vs-authoring) that `flow-testing` depends on.
- `lens-test-quality-reviewer.md`'s only path to a HIGH-severity finding directly contradicted its own Handoff-only rule for production bugs — literally unexecutable as written.
- `flow-testing` promised the reviewer the `flow-spec` artifact, a testing-guide reference, and `tests-developer`'s repair-vs-authoring answer — none of which its own dispatch briefing actually included.
- Mutation Verification (the mechanism proving a test actually verifies something) was completely invisible to the one reviewer meant to provide the independent check `flow-testing`'s own rationale describes — no stated reason why.
- `tests-developer.md`'s `color: pink` collides with the `{tech}-reviewer` family's deliberate role-marker color — traced to genuine palette exhaustion (all 10 documented colors are now used across the 10 developer agents), not an accident.

All of the above were fixed. A second re-review round then caught the expected second-order gaps — a "TWO checks" claim that still said "ONE" after `repair-weakening` was added as a new category, a new HIGH-severity path (a masked production defect) that had nowhere to go since `tests-developer` is structurally forbidden from touching production code, a carried-forward blocker obligation stated at the point it's created but not at the point it should be discharged, and a few wording-consistency nits. All fixed in a second pass; a third re-review would be checking convergence at this point, not finding new architecture bugs.

**Side effects on shared skills** (touched because the three-way contract required it, not spontaneously): `build-report-standards/SKILL.md` (the envelope-extension clause) and `review-boundaries/SKILL.md` (unrelated stale text caught in an earlier round, see Round 6).

This closes task tracking item for the test trio. 12 more identified gaps remain queued (see the session's task list): the other 8 flow skills, the 7 specialist agents, `review-core`/`review-report-standards`/`review-boundaries` as primary audit targets in their own right, the `templates/tech-pair/*` reconciliation, and the 25-item pre-existing backlog.

**Standing lesson:** at this depth, re-review is not optional — every round of fixes reliably introduces a small number of new wiring gaps, and this session's evidence is that it takes roughly three rounds per file family before the finding rate drops to pure cosmetics. Future work on this framework's agent-definition files should budget for that.

## 2026-09-18 — Test trio, rounds 3-7: the case-1/deferred-reviewer composition thread

**Method:** four more `lens-consistency-reviewer` re-review rounds on the same three files, requested specifically to re-check the fixes above. The prior entry's prediction ("a third re-review would be checking convergence, not finding new architecture bugs") was wrong for one specific thread: routing a masked-defect/implementation-wrong blocker out of `flow-testing` to `flow-implementation` turned out to name a re-entry case (`§0 case 3`) whose own stated precondition — "the `{tech}-reviewer` has already joined" — is false on exactly the Validate-First path that most often produces the blocker.

Fixing that one routing error took 5 rounds to fully close, each round's fix exposing the next corner: rerouting to case 1 instead (round 3) → disclosing the new loop-entry override in the human-approved gate template and reconciling a bare vs. qualified `§`/case citation (round 4) → discovering the case-1 discharge claim had two missing preconditions and an ambiguous summary-replacement instruction (round 5) → widening those preconditions consistently across both blocker-exit points, §4b and §5 (round 6) → finding the fix was still one-sided (`flow-implementation`'s own §4c/Invariant never got the reciprocal exception) and adding it there too, plus fixing one final Invariant-list compression that had dropped the conditionality (round 7).

**Net changes this round:** `flow-testing/SKILL.md` (§2, §3's gate template, §4b, §5, §6, Invariants — the routing/discharge mechanism now precondition-gated and stated consistently at all four sites) and `flow-implementation/SKILL.md` (§4c gained a new "Exception" paragraph + its Invariant amended) — a deliberate, narrow edit to a file otherwise queued as its own audit target (task #34), made only because this composition required both sides of the seam to agree.

**Round 7 verdict:** APPROVED_WITH_FOLLOWUPS, 1 residual LOW/MEDIUM (an invariant-list compression), fixed inline without a further dispatch round given the reviewer's own assessment that the mechanism itself is now "symmetric, bidirectional, and precondition-gated at every one of the four load-bearing sites" and the residue was a checklist-wording lag, not a design gap. Broader sweep of the trio (independent of this thread) found nothing further.

**Confirms the standing lesson above, sharpened:** a *cross-flow* composition point (not just intra-file wiring) can take noticeably more than three rounds when the fix itself introduces new surface each round — the ordinary "2-3 rounds to cosmetics" budget assumes the residue shrinks monotonically, which held for every other file group in this engagement but not for this one thread.

## 2026-09-18 — `flow-implementation`, task #34: first full audit

**Method:** 3 rounds of `lens-consistency-reviewer` full audit (not diff-scoped — this file had never been reviewed this engagement) on `flow-implementation/SKILL.md`, the primary "build code" orchestrator every other flow re-enters or hands off to/from.

**Round 1 (11 findings, 6 MEDIUM/5 LOW):** the frontmatter description omitted the Validate-First/Pair-First split; the Correctness-floor invariant was unconditional despite §2's own execution-test carve-out for framework prose; §0's case-3 re-entry rationale ("the reviewer has already joined") was stated as universal even though a `flow-testing`-originated blocker legitimately re-enters via case 1 where it hasn't; the review-only variant (§6) was internally inconsistent three ways (Seats block still listed a developer, the fix loop had no route for its `CHANGES_REQUIRED` with no developer seated); `flow-review`'s own Validate-First-availability rule was never restated here despite this file owning that window; a naming/wording drift ("Handoff-to-Reviewer note" vs. the canonical "Handoff to reviewer"); and a self-contradiction where "Round 1 is GUARANTEED" sat directly above the very condition that gates it (shared verbatim with `flow-testing`, fixed in both).

**Rounds 2-3:** the expected residue — each fix's own scoping needed a second pass (an over-generalized carve-out that wrongly swept in the cross-repo variant; an overstated claim that `flow-review` "will not accept" a pre-review diff when it actually can if the `{tech}-reviewer` is seated there explicitly; a stale Invariants bullet in both files left behind by the round-1 loop-block fix; an inverted-emphasis parenthetical). Round 3 found the two files' shared Invariants wording could not actually be identical — `flow-testing` has a loop-entry override `flow-implementation` doesn't, so the "symmetric" fix from round 2 was accurate in one file and wrong in the other. Fixed with an asymmetric final edit.

**Verdict:** `flow-implementation/SKILL.md` converged at round 3 (reviewer's explicit words: "has converged... nothing further in my lens"). Total: 16 findings across 3 rounds, all fixed, none deferred.

## 2026-09-18 — `flow-review`, task #35: first full audit, plus a real regression caught mid-pass

**Method:** 3 rounds of `lens-consistency-reviewer` full audit on `flow-review/SKILL.md` (the on-demand lens-swarm orchestrator) plus its `scripts/*.sh` and `tests/run-tests.sh` — first-ever audit this engagement, at the user's explicit request for extra rigor.

**Round 1 (11 findings, 8 MEDIUM/3 LOW):** the Security-floor `(hard)` rule and two other body obligations were missing from the Invariants list; §5b's "render per Rendering 1" had no stated destination (neither the human view nor the persisted artifact actually used it); the dispatch brief to each lens omitted two consequential fields (that correctness had already closed, and whether tests were expected) — both omissions cause real false-alarm disclosures downstream; the "seat the `{tech}-reviewer` here explicitly" alternative never stated whether it discharges `flow-implementation`'s own carried obligation (it doesn't — confirmed against that skill's actual Invariants); roster-derivation instructions were framed only around a diff, with no rule for a full-codebase/named-subset scope; the frontmatter dropped a real branch of its own Validate-First rule; and a template gap meant reviewer disclosures (out-of-scope findings, `review-boundaries` carve-outs) had nowhere to land. Two stale-citation bugs were also found and fixed directly in `render-md.sh`.

**Round 2 (4 resolved-cleanly, 4 new, plus one critical handoff):** the round-1 fixes were sound in prose, but two of them — the phantom "(of 3 max)" round-cap removal and a new artifact-Notes-section requirement — **broke a live, byte-exact golden-fixture test suite** (3 failing assertions) and asserted a persistence mechanism the schema/renderer genuinely can't support. This was the moment the user asked directly: *"you are fixing tests with test dev right"* — the honest answer was no, I'd been hand-editing the test file myself, which is exactly the ownership rule this whole engagement had been enforcing on every other agent. **Corrected the process, not just the file:** reverted my direct test edit, then routed the actual repair through `tests-developer` (verified: original fixtures genuinely failed pre-repair, repair passes, and the repaired assertions were mutation-checked to confirm they weren't accidentally weakened), followed by the mandatory `lens-test-quality-reviewer` pass per `flow-testing`'s own rules. That review found 2 more non-gating findings (missing multi-round coverage, an over-claiming test name) — extended to a full fix round: a new test genuinely exercising 2-round rendering, verified against two independent mutations of the renderer logic, both caught. Final `lens-test-quality-reviewer` verdict: **APPROVED, zero findings.** Also separately caught: round 1's own "empty findings → show a placeholder" fix was a false positive — an existing, deliberate, passing test proved the file's real convention is the opposite (bare heading, matching the GTD inbox renderer's own precedent) — reverted.

**Round 3:** confirmed the doc-side fixes landed, with one final one-line residue (a duplicated `(§0/§3)` citation) — fixed. Final state: `flow-review/SKILL.md` + its scripts converged; `run-tests.sh` at 203/0 passing (started at 201/0, +2 tests from the elective fix round).

**Process lesson, distinct from the earlier ones:** this file's test-suite side needed the SAME fix-and-verify discipline this engagement has been auditing into every other reviewer/flow all along — and the orchestrator (me) briefly broke that discipline itself before being caught by the user. Recorded here because it's the one place this session's own conduct, not just the framework's prose, needed correcting.

## 2026-09-18 — `flow-review`, task #35: doc audit, then a full 5-lens swarm on its scripts found a real HIGH

**Part 1 — doc audit (3 rounds, as usual):** `flow-review/SKILL.md` went through the same pattern as the other flows — 8 findings round 1 (missing Invariants entries for a `(hard)` floor, a dangling render-target reference, a convergent-finding signal silently discarded, an unstated cross-flow discharge relationship), 2 residue findings round 2, converged round 3.

**Part 2 — the user asked for a genuine lens swarm ("Lenses... just to make sure it's pristine") on the flow's own persistence/render scripts, never before audited by a language specialist.** `shell-script-reviewer`, `lens-security-reviewer`, `lens-clean-code-reviewer`, and `lens-self-documenting-code-reviewer` ran in parallel on the 4 production scripts (`render-md.sh`, `review-create.sh`, `review-add-round.sh`, `review-update-status.sh`). Combined: 42 findings, heavily convergent across reviewers (a strong signal). Headline: **`lens-security-reviewer` returned CHANGES_REQUIRED with one HIGH** — a multi-document `--fields-file` could bypass validation entirely, because jq's `-e` exit status reflects the LAST JSON document in a file while the build step reads the FIRST, letting an unvalidated (or maliciously shaped) finding get silently persisted while the artifact still reported `APPROVED`.

**Fix cycle (routed through the correct tech pair, not hand-edited — the lesson from earlier in this session held):** `shell-script-developer` fixed all 22 deduped findings in one batch (the HIGH, several fail-open validation guards, unconditional `chmod 644` overriding the caller's umask, a TOCTOU on the "refuse to overwrite" guard, a symlink path-escape, markdown-structure injection via untrusted finding text, plus 4 clean-code/doc-accuracy items) — extracting shared verdict-arithmetic into a new `scripts/lib/review-aggregates.jq` along the way. This broke 7 existing tests, honestly surfaced as expected consequences of the real fixes, not regressions.

**Two more rounds of round-2 VERIFY found residue in the fixes themselves** (7 more findings — a fail-open edge in a newly-added round-monotonicity check, a severity-domain filter that silently dropped tampered values instead of failing closed, etc.) — fixed, then re-verified clean by both `shell-script-reviewer` (APPROVED) and `lens-security-reviewer` (APPROVED_WITH_FOLLOWUPS, 1 trivial LOW — closed same-day). Notably, the developer independently tested the security reviewer's own suggested fix for one finding against a real CommonMark reference implementation, found it didn't actually work, and used a stronger mechanism instead — caught and confirmed correct on re-review.

**Test suite grew from 201 → 437 tests** across three `tests-developer` dispatches (repair the 7 broken-by-design tests; author coverage for the security-critical fixes that had none; close out coverage gaps a `lens-test-quality-reviewer` pass found in that new coverage) — every new/repaired assertion mutation-verified, every dispatch's production-file touches confirmed byte-identical (SHA-256) to their fixed state afterward.

**Final state:** `flow-review/SKILL.md` + all 4 scripts + `lib/review-aggregates.jq` + `run-tests.sh` (437/0 passing) — converged. Nothing committed.

## 2026-09-18 — `flow-tech-pair`, task #38: first full audit, 3 rounds, converged

17 findings across 3 rounds (12 → 4 → 1), all fixed. Highlights: the gate template disclosed none of its dispatched agents (the most of any flow — a research swarm, both meta-authoring generators, and 3 pre-deploy lens seats); the collision check couldn't see the existing `standards/language/` tier, so it would have missed that `standard-typescript` already exists; fix-routing said "fix directly" with a rationale that didn't actually hold (both generators exist and hold Write — routed to them instead, matching the repo's uniform pattern); and a hedge on the deploy step's dry-run behavior was corrected to state a guaranteed hub safety behavior as fact. Round 2's residue was entirely inside the new fix-routing paragraph (a missed blast-radius re-check, missing stable-ID pass-back, a hardcoded agent count) — fixed and converged at round 3.

## 2026-09-18 — `flow-decision`, task #39: first full audit, 2 rounds, converged

13 findings fixed (9 round 1, 4 round-2 residue — all pointer/wording hygiene). Highlights: §0 literally contradicted the file's own "3 lawyers is the ceiling" rule by saying panels spawn "3-4 lawyers"; the arbiter dispatch was missing 2 of its 5 declared required inputs (plan-vs-implementation flag, prior-verdict handback on re-dispatch); the gate template disclosed no loop bound or post-panel outcome; and the `flow-spec` caller relationship was one-directional (that flow says a panel's resolution feeds its draft, but this file never named the trigger or a return path) — now wired both ways.

## 2026-09-18 — `flow-external-review`, task #40: first audit, 4 rounds, a HIGH that took 3 rounds to actually close

Started CHANGES_REQUIRED (round 1: 1 HIGH + 11 MEDIUM + 3 LOW). The HIGH: `ESCALATED` is a first-class disposition but the published response, the summary, and the checklist never named it — an undecidable finding could disappear from the report entirely instead of reaching the human as the one thing the run couldn't settle. Fixed round 1 — but the fix itself (routing `ALREADY_RESOLVED` findings around the advocate seats that structurally can't express that verdict) broke a downstream gate and bypassed the one agent (`review-arbiter`) whose own body claims that ruling — a fresh HIGH, found in round 2. Round 2's fix then used the wrong field name from the arbiter's actual schema (`already_resolved_by` vs the ledger's `resolved_by_commit`) — the same HIGH re-opened a third time in round 3, this time for a genuinely different reason. Round 4 finally verified it closed by reading the actual schema files directly rather than trusting the fix description, and confirmed: no third field name exists anywhere in the repo, and every link in the `ALREADY_RESOLVED` chain (flag → arbiter dispatch → gate → disposition table → report → render → checklist) agrees.

Also touched: `contracts/external-review-ledger.schema.json` (added a missing `escalation_blocker` field so the ESCALATE path's one useful datum — the judge's stated blocker — actually has somewhere to live; fixed two stale rule-id citations and two optional-field descriptions that contradicted the schema's own `required[]`).

**Lesson repeated from `flow-review`:** a fix that lands correctly at the site a finding named can still leave an unswept sibling site with the old, now-contradictory text — this file restates several rules (its own asks, its exceptions, its temp-file discipline) at 3-4 sites each by design, and each round's residue was almost entirely incomplete propagation, not a new idea being wrong.

## 2026-09-18 — `flow-documentation`, task #41: first audit, 3 rounds, converged

13 findings fixed across 3 rounds (9 → 3 → 1, plus 3 proactive polish fixes). Notable: this was the only flow in the entire audit with NO gate before dispatching its write-capable agent (`tech-writer`, Write/Edit/acceptEdits) and NO Invariants block at all — both added. Also caught: a two-valued verdict vocabulary against the reviewer's actual three-valued contract, a doc-type vocabulary mismatch between what's passed to `tech-writer` and what it reports back (5 ad-hoc types vs. its real 4 Diátaxis modes), and the `{tech}-reviewer` being dispatched to fact-check documentation with no acknowledgment that no reviewer body actually supports that use — now briefed as an explicit, deliberate lens deviation.

## 2026-09-18 — `flow-git-operations` + `git-operator`, task #42: 8 rounds, the deepest audit of this engagement

Flagged by the user as critically important (real commit/push/PR/MR mechanics) with an explicit "be extremely diligent" instruction. Scope grew from the flow file alone to include `git-operator.md` (the agent it briefs) once round 2 found the two files disagreeing with each other — and stayed there because each fix kept leaving one more contradictory sentence in unedited-looking prose nearby.

**Baseline discipline:** before touching anything, ran all 6 real test suites this work depends on (`procedure-git-ops`, `procedure-git-identity`, `procedure-gh-pr`, `procedure-glab-mr`, `procedure-github-auth`, `procedure-gitlab-auth` — 1,142 checks total) to establish a trusted baseline, then re-ran them after every round of edits. Stayed green throughout — every fix this round was prose-only, but the check was never skipped given the stakes.

**What actually mattered:**
- **Round 1** found real mechanics errors verified against the actual `.sh` scripts, not just the prose describing them: a `commit.sh --signoff` flag that doesn't exist, an entirely undocumented Branch Path (a declared trigger phrase with zero procedure behind it), a multi-commit split with no way for the orchestrator to re-stage units 2..N, and a required `--confirmed-host` flag with no pre-flight step to obtain it.
- **Rounds 3, 4, and 6 each found a genuine HIGH** — the same defect shape recurring three times: `git-operator.md`'s Constraints/Staging/conduct prose kept instructing the operator to run scripts it is structurally forbidden from running (`commit.sh`, `push.sh`, the account-confirmation gates), directly contradicting the plan-only doctrine stated elsewhere in the same file. A literal reading of any of these would have produced an unconsented, signed, possibly mixed-concern commit.
- **Round 4's fix introduced the structural countermeasure**: the frontmatter description now states the boundary *by role* ("every script that writes history or reaches a remote is the orchestrator's") rather than by enumerating script names — closing the entire class of "one list updated, a sibling list missed" defect that had caused every prior HIGH.
- **Round 6 found a real flow-level gap**, not just wording: the GitLab account-gate pre-flight was scoped only to the Merge-Request Path, so every plain GitLab commit's identity check was silently capped at `unknown` by construction. Fixed by widening the pre-flight to any GitLab-remote operation, then reconciling a halt-vs-fail-open policy split and an account-gate amortization gap across three call sites in round 7.
- **Round 8 converged** — `APPROVED_WITH_FOLLOWUPS`, 0 gating findings. One tracked, deliberately out-of-scope follow-up remains: `procedure-gh-pr` and `procedure-glab-mr`'s own `SKILL.md` files still carry the pre-split "the operator opens/edits" framing in their headers (their bodies already have the correction) — flagged for whoever next touches those two files, not treated as reopening this round.

Total: ~35 findings across 8 rounds on two files, all fixed, test baseline never broken.

## 2026-09-18 — 6 specialist agents, task #43: a 24-agent 4-lens swarm, 3 rounds, 7 genuine HIGHs

**Scope:** `decision-arbiter.md`, `review-arbiter.md`, `software-architect.md`, `tech-developer-generator.md`, `tech-reviewer-generator.md`, `tech-writer/tech-writer.md` — the 6 remaining `specialists/*` files never touched this engagement (`git-operator` was closed at task #42). At the user's explicit direction, treated as a genuine multi-lens swarm rather than the single-`lens-consistency-reviewer` pass used on the flow files: 4 lenses per file (`lens-consistency-reviewer`, `lens-clean-code-reviewer`, `lens-self-documenting-code-reviewer`, `lens-security-reviewer`) run fully in parallel — 24 agents for round 1 alone.

**Round 1 found 7 genuine HIGH security findings, one per file except `tech-writer.md` (3):** every file holding `WebFetch`/`WebSearch` had zero untrusted-content-as-data discipline in its body. The two meta-authoring generators compounded this into a real injection-to-persistence path: unverified web research could be cited as fact directly into a *shell command block* a generated `{tech}-developer` would later execute, or into the *gating correctness criteria* of a permanently-deployed `{tech}-reviewer`, with no corroboration requirement either way. `software-architect.md` additionally carried a 12-tool, entirely unbounded `mcp__github-server__*` grant with zero found consumers anywhere in the repo. `tech-writer.md`'s redaction discipline covered person names/tickets/paths but not credentials, and its write-scope was bounded by file *type* (`*.md`) rather than path — legally permitting it to edit any `CLAUDE.md` or agent-definition file in the repo.

**A repeat-finding color collision, confirmed independently by 3 parallel dispatches:** `color: purple` was a 5-way collision (`kotlin-developer`, `lens-clean-code-reviewer`, `decision-arbiter`, `review-arbiter`, `tech-writer`) against the repo's own "one color per {developer, lens-reviewer, specialist} triple" pattern (4 already-clean triples proved it deliberate). Resolved: `decision-arbiter` kept `purple` (completes that triple), `review-arbiter` → `teal`, `tech-writer` → `magenta` (ties to the self-documenting-code lens). `color: blue`'s 5-way collision (`software-architect` + `project-manager` + `gtd-inbox-writer`, the latter two outside `software-development/`) was confirmed but deliberately left unfixed — cross-subsystem, out of this task's scope.

**Round 2 (12 dispatches — security + consistency re-verification on all 6 files) closed nearly everything but surfaced real second-order gaps, the strongest of which:**
- `review-arbiter.md`'s new fail-secure rule forbade emitting `NOT_AN_ISSUE` — a ledger *disposition*, not a value in this agent's own verdict domain — leaving the actual dismissal value (`FALSE_POSITIVE`) unnamed and the fail-open route the rule existed to close still open. Caught independently by both the security AND consistency re-dispatches on the same file — a strong convergence signal.
- `decision-arbiter.md`'s hardening paragraph (ported from its sibling) covered fetched/artifact content but omitted the panel reviews and the prior arbiter verdict themselves — its *primary* mandated inputs — plus had no SSRF constraint, no fail-secure-on-doubt tilt, and no credential/path-containment rule, all already present on `review-arbiter.md`. Also: folding "When to trigger" into the lead sentence made it the only one of 35 agent files in the repo without that section.
- `software-architect.md`'s new provenance-disclosure duty landed only on the direct-consult ADR path; both flow-seat schemas are `additionalProperties: false` with no provenance field, so an external/unverified claim still arrives at the arbiter indistinguishable from an in-repo-verified one. Its new github-server purpose-bound text also had no matching input in "How to prompt", making the grant currently unreachable by any well-formed dispatch either way.
- Both generators: `tech-developer-generator.md`'s new corroboration rule was sink-scoped to commands/package names, missing a security-relevant *idiom* (e.g. a stale TLS-off pattern) codified as fact; `tech-reviewer-generator.md`'s composed-standards clause omitted `standard-security`, contradicted by both real precedents (`react-reviewer`, `cloudflare-workers-reviewer`) that bind it; neither generator's write-guard fully matched the other's (one had an exists-check, the other didn't; slug regexes differed).
- `tech-writer.md`: the redaction-envelope wording had gone stale against its own broadened categories, and understated that credentials/PII have *no* backstop beyond the agent's own pass (unlike ticket-IDs/paths, which the external lint gate also covers).

**Round 3 closed the round-2 residue directly** (no further dispatch, per the user's 3-cycle cap) — ported the missing hardening paragraphs between the two arbiters, fixed the `FALSE_POSITIVE` vocabulary bug, added the sixth "external repo in scope" input to `software-architect.md` plus a disclosure channel for the closed-schema seats, extended both generators' corroboration/composed-standards/write-guard rules to match each other, and brought `tech-writer.md`'s envelope and fetch-origin text in line with its own actual (broader) rules.

**Deliberately deferred, not fixed here:** the `blue` color collision (cross-subsystem); `template-tech-developer.md`/`template-tech-reviewer.md` themselves, confirmed (again, independently, by 4+ dispatches across this and earlier rounds) to be the actual root source of the stale "3-4 examples" / "eight handoff targets" claims both generators had to work around with a precedence rule rather than a citation — this is task #45's job; and a `flow-documentation` two-hop residual (`tech-writer.md`'s new file-list self-report has no mechanical blast-radius backstop the way `flow-tech-pair` has for its generators).

Total: ~90 findings across 3 rounds and 6 files (24 + 12 dispatches), all HIGH/MEDIUM fixed or explicitly deferred with reason, nothing left silently open.

## 2026-09-18 — review-core, review-report-standards, review-boundaries, task #44: the 3 most load-bearing files in the whole engagement, 3 rounds, 2 genuine HIGHs

**Scope:** `shared/review-core/SKILL.md`, `shared/review-report-standards/SKILL.md`, `shared/review-boundaries/SKILL.md` — the three foundational contracts every reviewer/lens/tech-reviewer/arbiter in the repo binds (conduct, report format, finding ownership respectively). Never audited as primary targets before this task — only individually cited and occasionally patched by other files' audits earlier in this engagement. Same 4-lens-swarm methodology as task #43: 9 dispatches round 1 (`lens-consistency-reviewer` + `lens-clean-code-reviewer` + `lens-self-documenting-code-reviewer` per file — no security lens, since these are pure rubric documents with no tool grants of their own), each explicitly briefed to cross-check the other two siblings and a sample of real consuming reviewer files, not just its own target.

**Round 1 found 2 genuine HIGH findings, both logic bugs baked into the framework's own rules, not style:**
- `review-report-standards.md`'s own **worked example** graded a pure coupling/DIP finding `HIGH` when the file's own severity rule two sections above says that class of finding is `MEDIUM`. Since a worked example in these instruction files is normative-by-imitation, every one of ~19 reviewers bound to this contract was being taught to wrongfully gate the fix loop on maintainability findings.
- `review-core.md`'s test-file tripwire ordered a "gating" (CRITICAL/HIGH) finding for a scenario (a developer editing its own tests) that the framework's own severity scale — owned by a *different* file — had no legal path to grade that high. A genuine contradiction between two rules in the same contract family, not a wording nit.

**The single most-confirmed defect in this entire engagement:** `review-core.md`'s Severity Philosophy section claimed "this section does not restate it — it points at it," then restated the four-tier scale anyway, and the copy had already silently drifted from the source (dropped the exact clause distinguishing `LOW` from `MEDIUM`, the boundary the verdict arithmetic depends on). **7 independent dispatches**, across both rounds and multiple lenses, flagged this same defect from different angles.

**Also real:** `review-report-standards.md` called its own default JSON rendering a "table" in 4 places when it's actually a bullet list (an actionable-wrong instruction under Constraints specifically); its Rendering 1 (human-only, secondary) was numbered and read before Rendering 2 (the actual default), inverting the newspaper rule on the hottest path in the file; its own worked example mislabeled a Rust-bodied finding as "Kotlin Reviewer" with a file count matching nothing in the example (confirmed 3+ times independently); its tech-prefix list was missing 2 of 9 deployed prefixes (`PYTHON`, `CLOUDFLARE`) — the exact staleness class already caught and worked around in `tech-reviewer-generator.md` earlier this engagement, now traced to its actual source. `review-boundaries.md`: 3 of 9 lens-reviewers (`lens-consistency-reviewer`, `lens-observability-reviewer`, `lens-security-reviewer`) never bound it at all despite explicitly disclaiming correctness territory in their own descriptions — and those same 3 were independently caught paraphrasing its ownership table uncited, the precise drift the document exists to prevent. A real governance gap: the `ACK` lifecycle let the *orchestrator*, not just the human, unilaterally waive a `CRITICAL`/`HIGH` finding with no consent step.

**Fixes applied directly (all 3 primary files fully rewritten), plus a necessary ripple:** the 3 non-binding lens files were given the `review-boundaries` skill binding, a `**Boundaries —**` description paragraph, and a Constraints citation; `flow-review/SKILL.md`'s conditional binding of `review-boundaries` was made unconditional, since the correctness-floor row is universal, not contested-only.

**Round 2 (9 dispatches, same 3-lens swarm) explicitly re-confirmed both HIGHs closed** — the `review-core` HIGH closed *reciprocally* (the exception is now granted by the scale's actual owner, `review-report-standards`, not just asserted unilaterally by the non-owner), and the worked-example HIGH closed with an internally-consistent, correctly-seated replacement finding. Surfaced real second-order gaps: my own fix widened rather than closed one duplication (the `flow-testing` trigger premise, restated twice, now 6 lines apart instead of 4); the severity-disqualifier list I'd moved out of `review-core` was only *partially* removed (a residual paraphrase survived); `review-report-standards`' own emit-JSON-default rule went from 4 restatements to *5* after my ACK-governance additions; the deployed JSON-Schema contract (`review-finding-report.schema.json`) has its own independent copy of the prefix list, already stale with the same omission just fixed in the skill file (confirmed by 2 separate dispatches); `review-boundaries.md`'s own fix over-corrected, restating the partition-vs-overlap distinction 3 times in one sentence; and one real MEDIUM propagation gap — the new 4th Notes-kind (`Unreviewed territory`) was added at one site in `review-report-standards.md` but two other sites still said "(all three kinds)".

**Round 3 closed all of the above directly** (no further dispatch, per the 3-cycle cap): trimmed the `flow-testing` duplication to one site; removed the residual severity-disqualifier paraphrase from `review-core`, leaving `review-report-standards` sole owner; consolidated the emit-JSON-default and schema-fact restatements in `review-report-standards` down to one home each with citations elsewhere; fixed the schema contract's own stale prefix description; fixed `review-boundaries`' over-corrected sentence and two heading-case deviations (sentence case vs. the family's Title Case) also caught this round; propagated the 4th Notes-kind count fix to both remaining sites.

**Deliberately left as noted, non-gating LOWs:** a handful of cross-reference/placement nits each lens explicitly graded LOW and none of the reviewers considered worth a 4th round — recorded in each dispatch's own findings if picked up later.

Total: ~110 findings across 3 rounds and 3 primary files (9 + 9 dispatches) plus the 4-file ripple (3 lens bindings + 1 flow line), both HIGHs fixed and independently re-confirmed closed, nothing left silently open.
